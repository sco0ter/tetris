import org.testng.annotations.Test;

/**
 * @author Christian Schudt
 */

@Test
public class TestRotation {

    public void testIShape() {
        //Tetromino tetromino = Tetromino.I;

        /*Assert.assertTrue(Arrays.asList(tetris.tetromino.get()).contains(new int[]{0, 0}));

        tetris.tetromino.rotateLeft();
        System.out.println("Rotating left...");

        for (int i = 0; i < tetris.tetromino.getCoordinates().length; i++) {
            System.out.println(Arrays.toString(tetris.tetromino.getCoordinates()[i]));
        }

        tetris.tetromino.rotateLeft();
        System.out.println("Rotating left...");

        for (int i = 0; i < tetris.tetromino.getCoordinates().length; i++) {
            System.out.println(Arrays.toString(tetris.tetromino.getCoordinates()[i]));
        }
        tetris.tetromino.rotateLeft();
        System.out.println("Rotating right...");

        for (int i = 0; i < tetris.tetromino.getCoordinates().length; i++) {
            System.out.println(Arrays.toString(tetris.tetromino.getCoordinates()[i]));
        }
        tetris.tetromino.rotateLeft();
        System.out.println("Rotating right...");

        for (int i = 0; i < tetris.tetromino.getCoordinates().length; i++) {
            System.out.println(Arrays.toString(tetris.tetromino.getCoordinates()[i]));
        }
        Assert.assertTrue(true);  */
    }
}
